# mcp capped <2: 2.0.0 removed the 1.x low-level Server decorator API
# (measured 2026-08-08: fresh install pulled 2.0.0 and server.py died with
# "'Server' object has no attribute 'list_tools'"). Floor = measured-working
# 1.26; the 2.x port is a deliberate migration, not a bootstrap side effect.
mcp>=1.26.0,<2.0.0
httpx>=0.27.0
