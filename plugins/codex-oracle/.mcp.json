{
  "mcpServers": {
    "codex-oracle": {
      "type": "stdio",
      "command": "${CODEX_ORACLE_PYTHON:-python3}",
      "args": ["${CLAUDE_PLUGIN_ROOT}/run_server.py"],
      "timeout": 14400000
    }
  }
}
